# Domain Docs

## Before Exploring, Read These

- `CONTEXT.md` at the repo root, or `CONTEXT-MAP.md` if it exists.
- `docs/adr/` for ADRs touching the area of work.

If these files do not exist, proceed silently. `/domain-modeling` creates them
only when terminology or decisions need recording.

## File Structure

```text
/
├── CONTEXT.md
├── docs/adr/
└── src/
```

## Consumer Rules

Use terminology defined in `CONTEXT.md`. If an output conflicts with an ADR,
